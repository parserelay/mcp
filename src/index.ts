// Library entry: build the MCP server programmatically and wire your own transport.
// For the ready-to-run servers, use the `parserelay-mcp` bin (stdio) or `dist/http.js`.
export { createMcpServer } from "./server.js";
